import React from 'react';

const Header = () => {
    return (
        <header className="app-header">
            <nav className="nav-links">
                <a href="#">Modules</a>
                <a href="#">Create</a>
            </nav>
        </header>
    );
};

export default Header;
